import MainLink from "@/components/mainLink";
import MainTitle from "@/components/mainTitle";

const Home = () => {
  return (
    <main className="main">
      <MainTitle />
      <MainLink />
    </main>
  );
};

export default Home;
